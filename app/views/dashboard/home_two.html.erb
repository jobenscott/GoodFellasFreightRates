
    <script>
     


		jQuery(function($){
			$('#test-submit').on('click', function(){
				var directionsService = new google.maps.DirectionsService;
				calculateAndDisplayRoute(directionsService);
			});
		});

      	function calculateAndDisplayRoute(directionsService) {
	        directionsService.route({
	          origin: document.getElementById('origin').value,
	          destination: document.getElementById('destination').value,
	          travelMode: 'DRIVING'
	          // trafficModel: 'BEST_GUESS'
	        }, function(response, status) {
	          if (status === 'OK') {
	            // directionsDisplay.setDirections(response);
	            // console.log(response);
	            var test_response = response['routes'][0]['legs'][0]['distance'];
	            console.log(parseInt(test_response['value'])*(0.000621371));
	          } else {
	            window.alert('Directions request failed due to ' + status);
	          }
	        });
	      }
    </script>
    <script async defer
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDZxbBGxzV2sy-yXu_9iLCBqnaVur5ww20">
    </script>